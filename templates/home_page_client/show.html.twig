{% extends 'base.html.twig' %}

{% block title %}HomePageClient{% endblock %}

{% block body %}
    <h1>HomePageClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_page_client.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ home_page_client.name }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ home_page_client.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_page_client_index') }}">back to list</a>

    <a href="{{ path('home_page_client_edit', {'id': home_page_client.id}) }}">edit</a>

    {{ include('home_page_client/_delete_form.html.twig') }}
{% endblock %}
