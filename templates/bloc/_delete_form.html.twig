<form method="post" action="{{ path('bloc_delete', {'id': bloc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bloc.id) }}">
    <button class="btn">Delete</button>
</form>
