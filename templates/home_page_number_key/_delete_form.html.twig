<form method="post" action="{{ path('home_page_number_key_delete', {'id': home_page_number_key.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_page_number_key.id) }}">
    <button class="btn">Delete</button>
</form>
