{% extends 'base.html.twig' %}

{% block title %}HomePageNumberKey index{% endblock %}

{% block body %}
    <h1>HomePageNumberKey index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ProductNumber</th>
                <th>StoreNumber</th>
                <th>PackageNumber</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for home_page_number_key in home_page_number_keys %}
            <tr>
                <td>{{ home_page_number_key.id }}</td>
                <td>{{ home_page_number_key.productNumber }}</td>
                <td>{{ home_page_number_key.storeNumber }}</td>
                <td>{{ home_page_number_key.packageNumber }}</td>
                <td>
                    <a href="{{ path('home_page_number_key_show', {'id': home_page_number_key.id}) }}">show</a>
                    <a href="{{ path('home_page_number_key_edit', {'id': home_page_number_key.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('home_page_number_key_new') }}">Create new</a>
{% endblock %}
